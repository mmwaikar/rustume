## ADDED Requirements

### Requirement: Compact language fluency labels
Language fluency values in the Profile view MUST be rendered as compact bubbles that are smaller than primary profile highlight bubbles and remain horizontally readable.

#### Scenario: Language with fluency
- **WHEN** a language entry has a fluency value
- **THEN** the language name is shown with its fluency in a compact, horizontally oriented bubble